# 试卷生成（JSON → Word）集成测试报告

- **测试日期**：2026-07-17
- **测试对象**：Phase 4 集成（UI 参数 → 解析 → 并发下载图片 → 生成题本/解析 → 写出）
- **环境**：Python 3.13（managed venv），PySide6 6.11.1，python-docx 1.2.0，pytest + offscreen
- **结果**：**89 passed**（含新增端到端/集成测试 11 项），mypy --strict（shared+core）无错误

## 一、集成改动概览

| 文件 | 改动 |
| --- | --- |
| `shared/contracts.py` | `GenerateExamResult` 新增 `failed_images: List[str]`（失败图片 URL） |
| `shared/errors.py` | 新增 `OutputWriteError`（输出目录无写入权限） |
| `core/services/_exam_image_fetch.py` | **新增**：`_fetch_image_bytes` + `prefetch_images`（线程池并发下载，上限 6，带重试与失败收集） |
| `core/adapters/docx_exam_writer.py` | 接受 `image_cache`；双文档共用一份缓存；统计 `failed_images`；写出失败抛 `OutputWriteError` |
| `core/services/json_to_word_service.py` | 主流程串联：解析 → 并发预下载（步骤进度 `下载图片 x/n`）→ 生成题本 → 生成解析，回传失败列表 |
| `core/ports/io.py` | `ExamDocxWriter.build` 端口增加可选 `image_cache` 参数 |
| `widgets.py` | **新增** `ErrorDialog`（全局错误弹窗，复用主题令牌） |
| `ui/viewmodels/json_exam_viewmodel.py` | `failed` 信号透传异常对象（便于视图按类型分流） |
| `ui/views/json_exam_view.py` | `_on_failed` 按 `DocumentReadError` / `OutputWriteError` / 其他 分流弹窗或 Toast；`_on_completed` 汇总失败图片报告 |

## 二、端到端测试（使用样本 `专项智能练习（数量关系）_2026-07-17T06-01-08-897.json`）

图片下载在测试中 mock 为本地 PNG 字节，保证离线/确定性。

### 场景 A — 默认排版参数生成
- 断言：输出文件存在且命名为 `专项智能练习（数量关系）_题本.docx` / `_解析.docx`
- 题数 = 3，`failed_images == []`
- 进度事件齐备：`解析中` → `下载图片 x/n` → `生成题本...` → `生成解析...` → `exam_completed`

### 场景 B — 自定义行间距 + 关闭首行缩进
- 参数：`line_spacing_type=CUSTOM, line_spacing_value=1.8, first_line_indent=False`
- 断言：题本首个正文段落 `line_spacing ≈ 1.8` 且 `first_line_indent = 0`（Pt）

### 场景 C — 模拟部分图片链接失效
- 让样本中约半数（索引为偶数）图片 URL 下载失败
- 断言：生成仍成功、文件完整；`failed_images` 与预期失败子集完全一致；
  进度栏出现「部分图片下载失败」警告；题本文档出现 `[图片加载失败: ...]` 占位框

## 三、其它新增测试
- `test_prefetch_collects_failures_offline`：验证 `prefetch_images` 并发收集失败 URL
- `test_permission_error_raises_output_write_error`：输出目录无权限时抛 `OutputWriteError`
- 视图集成（offscreen）：`_on_progress` 步骤文案/进度条、`_on_completed` 失败报告弹窗、
  `_on_failed` 三类异常的弹窗/Toast 分流

## 四、真实样本冒烟验证
直接对样本运行真实生成（联网）：
- 17 张图片并发下载（`下载图片 1/17` … `17/17`）
- 步骤事件与预期一致，3 题全部图片成功，`failed_images` 为空
- 输出文件名正确

## 五、结论
完整闭环（点击生成 → 两份 docx）已打通；并发下载、步骤化进度反馈、失败图片报告、
以及 JSON/权限/图片异常的错误处理均按需求实现，UI 风格复用项目全局组件，
与现有工具保持一致。所有测试通过，类型检查干净。
